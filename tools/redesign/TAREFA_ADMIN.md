<task>
TAREFA DE IMPLEMENTAÇÃO (modo escrita: você deve criar e editar arquivos de verdade, não só opinar).

Projeto: /home/edivaldo/PycharmProjects/SistemaEXTREMETEAM (Flask 3 + Jinja + SQLAlchemy 2, CSS/JS puros, branch `redesign-et`, árvore de trabalho compartilhada com outros agentes).

PASSO 0 — leia, nesta ordem, antes de qualquer edição:
1. REDESIGN_CONTRATO.md (adaptações aprovadas, contrato de design, restrições de CSP/CSRF, DONO DE CADA ARQUIVO, regras de convivência, formato do relatório).
2. PROMPT_CLAUDE_CODE.md (a especificação do dono do projeto; siga-a — as adaptações do contrato têm prioridade onde divergem; ignore tudo que fale de Supabase).
3. static/css/tokens.css, static/css/componentes.css, templates/components/ui.html e as primeiras ~140 linhas de static/css/theme.css.
Para se orientar no código use o grafo ANTES de ler arquivos soltos: `graphify query "<pergunta>"` / `graphify explain "<conceito>"` / `graphify path "<A>" "<B>"` (ex.: "painel_financeiro totais_periodo", "_situacoes_dos_ativos", "PresencaDAO"). NÃO rode `graphify update`. NÃO use comandos git que escrevam.

SUA FATIA: o PAINEL ADMINISTRATIVO — telas 8, 9, 10 e 11 do prompt-mãe, mais uma página de Relatórios. Você é dono SOMENTE de: templates/pgAdm.html, dt_aluno.html, financeiro.html, admin_aluno_novo.html, admin_avisos.html, admin_academia.html (⚠ alterações não commitadas do dono: rode `git diff` nele, no admin_academia.css e em blueprints/academia_bp.py e preserve tudo; restile sem mexer na lógica de conexão do Mercado Pago); static/css/pgAdm.css, dt_aluno.css, financeiro.css, admin_academia.css (⚠); static/js/pgAdm.js, filtros_financeiro.js, mensalidades_filtro.js; blueprints/adm_bp.py; dao/*.py (SÓ ACRESCENTAR funções novas; não altere as existentes); e arquivos NOVOS: templates/admin_alunos.html, admin_relatorios.html, components/graficos.html, static/css/admin_paginas.css, tests/test_admin_*.py. A sidebar (components/admin_nav.html, admin_nav.js e o CSS do shell) é do orquestrador: ele vai incluir os itens Painel, Alunos (/admin/alunos), Turmas, Financeiro, Planos (/admin#planos), Avisos, Relatórios (/admin/relatorios), Academia. Suas páginas novas incluem o menu como as existentes fazem (`{% include 'components/admin_nav.html' %}`, com `{% set secao_ativa = 'alunos' %}` / `'relatorios'` — veja o padrão em pgAdm.html). Dê `id="planos"` à seção "Planos da academia". Componentes que não são seus (plano_situacao, mensalidade_acao, paginacao, comprovante_modal…): apenas inclua; mudanças vão em "Pedidos ao orquestrador".

O QUE ENTREGAR
8) Painel (`/admin`): linha de cards de métrica com a macro `metrica` de ui.html — Alunos ativos, Receita do mês (recebido no mês corrente), Pagamentos pendentes (quantidade e valor), Presença hoje, Aguardando aprovação. Lista "Cadastros pendentes" com Aprovar/Recusar (rotas já existem: preserve-as e os formulários/CSRF/confirm-modal). Tabela "Planos da academia" + "Cadastrar plano" (preserve). Dois gráficos em SVG renderizado no servidor (a CSP proíbe CDN/Chart.js): barras "Receita dos últimos 6 meses" e linha de alunos. ATENÇÃO à linha "Total de alunos": o modelo Aluno pode não ter data de cadastro; NÃO invente histórico. Verifique (`graphify explain "Aluno"`, modelos/usuario.py, modelos/pagamento.py). Se não houver série histórica confiável, derive dos intervalos de vigência das mensalidades pagas (`Pagamento.vigencia_inicio/vigencia_fim`) a série "Alunos com plano ativo" ao fim de cada mês, e rotule assim, honestamente. Cada gráfico tem título, eixo legível, legenda e uma tabela equivalente acessível (visível ou `visually-hidden` — confira se já existe classe utilitária em theme.css). Crie os macros em components/graficos.html e as consultas como funções NOVAS de DAO (agregando em SQL; sem N+1; sem concatenar SQL com entrada do usuário). A seção "Alunos" que hoje mora no painel passa a ser um resumo com link "Ver todos os alunos" para /admin/alunos — antes, `grep -rn` nos testes para não quebrar o que já é verificado em /admin.
9) Alunos (`GET /admin/alunos`, NOVA rota com `@admin_requerido`, como as demais de adm_bp.py): busca `q` por nome, e-mail ou telefone (LIKE com parâmetro escapado; ignore mascara/pontuação do telefone), filtros por plano e por status (ativo, inativo, aguardando aprovação, recusado — use os campos reais de Aluno: `status_cadastro`, `ativo`), paginação (templates/components/paginacao.html já existe: veja como financeiro a usa), tabela com foto/nome, plano, situação (badge), telefone e botão "Ver perfil" (→ /admin/usuario/<cpf>). Estado vazio escrito ("Nenhum aluno encontrado com esses filtros"). Link "Cadastrar aluno" para /admin/alunos/novo. Uma consulta agregada por página, nunca uma por aluno.
10) Perfil do aluno (dt_aluno.html): foto, badges de situação e plano, dados pessoais, turmas, situação do plano, "Controle de mensalidades" (Atualizar, Aprovar comprovante, Rejeitar — já existem), "Enviar cobrança por e-mail" e "Editar cadastro". Reorganize a hierarquia e restile; NÃO mude rotas, `name=` nem a lógica.
11) Financeiro (financeiro.html): cards com `metrica` — Recebido no período, Pendente, Vencido (é o `atrasado` do banco; exiba "Vencido"), Em análise, Alunos inadimplentes; filtros de período, turma, plano, método, situação e aluno (mantenha os que existem; acrescente só os que forem baratos e corretos no DAO, como funções novas); tabela com "Ver detalhes" e destaque `.btn-destaque` "Revisar comprovante" nas linhas em análise. Preserve a paginação, os indicadores que os testes verificam e a concordância indicadores×tabela (há testes de regressão sobre isso).
RELATÓRIOS (`GET /admin/relatorios`, nova rota, `@admin_requerido`, `secao_ativa='relatorios'`): página enxuta com três blocos de dados reais — receita por mês (últimos 12), inadimplência por plano (valor e quantidade de vencidos) e frequência por turma (presenças nos últimos 30 dias / matriculados). Cada bloco com tabela e, onde ajudar, SVG via components/graficos.html. Estados vazios escritos.
Também restile, com mão leve, admin_avisos.html, admin_aluno_novo.html e admin_academia.html para a nova linguagem (cartões escuros/creme, títulos Bebas, botão dourado, badges).

QUALIDADE: valores com `|moeda` (R$ 1.234,56), datas dd/mm/aaaa (fuso America/Fortaleza: veja se já há helper em servicos/formatacao.py antes de criar outro), toda rota nova protegida por `@admin_requerido` no servidor, entradas validadas (inteiros de filtro com fallback, página ≥ 1), saída escapada pelo Jinja. Respeite a CSP (sem handlers inline) e o CSRF em todo POST.

VERIFICAÇÃO (não suba o servidor; use pytest): crie tests/test_admin_painel_redesign.py cobrindo: /admin, /admin/alunos (busca, filtros, paginação, estado vazio, acesso negado a aluno/professor/anônimo → redirect /login) e /admin/relatorios; e testes das funções novas de DAO (valores conferidos com dados criados via `criar_aluno`/`criar_pagamento`). Rode `env -u DATABASE_URL .venv/bin/python -m pytest -q tests/test_admin_painel_redesign.py tests/test_financeiro_admin_painel.py tests/test_financeiro_dao.py tests/test_regressao_auditoria.py tests/test_cadastro_administrativo.py tests/test_academia.py` e depois a suíte completa (`env -u DATABASE_URL .venv/bin/python -m pytest -q`, linha de base 589 passed / 8 skipped). Antes de mudar qualquer texto/atributo: `grep -rn "<texto>" tests static/js`.

Termine com o relatório no formato da seção 6 do REDESIGN_CONTRATO.md, em português.
</task>

<completeness_contract>
Entregue tudo o que está em "O QUE ENTREGAR" (telas 8, 9, 10, 11, Relatórios e o restyle leve das três páginas) mais os testes. Não pare em um plano ou em uma implementação parcial: continue até implementar, rodar os testes e corrigir as falhas causadas por você.
</completeness_contract>

<verification_loop>
Depois de implementar, rode os pytest indicados e a suíte completa. Se algo falhar por causa das suas mudanças, corrija e rode de novo. Não declare concluído sem evidência de execução; reporte os números reais (passed/skipped/failed) e diga claramente qualquer falha pré-existente ou causada por outro agente.
</verification_loop>

<action_safety>
Edite somente os arquivos listados como seus em SUA FATIA (a árvore de trabalho é compartilhada com outros agentes). Não altere funções existentes de dao/*.py, só acrescente novas. Não escreva em git (sem commit, add, checkout, reset, stash). Não rode `graphify update`. Não suba o servidor. Preserve as alterações não commitadas do dono em admin_academia.html, admin_academia.css e blueprints/academia_bp.py, e não mexa na lógica de conexão do Mercado Pago. Pedidos de mudança em arquivos de outros donos vão na seção "Pedidos ao orquestrador" do relatório.
</action_safety>

<missing_context_gating>
Não invente dados históricos nem campos inexistentes: verifique nos modelos antes de usar. Se algo essencial estiver ambíguo, escolha a opção mais conservadora, siga em frente e registre a decisão no relatório.
</missing_context_gating>
